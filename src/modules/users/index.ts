/**
 * @module users
 *
 * Public surface for the users module. Anything outside this folder
 * MUST import from `@/modules/users` (this file) — never reach into
 * `./service`, `./schema`, or `./policies` directly. ESLint enforces it.
 */
export {};
