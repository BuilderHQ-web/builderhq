/**
 * @module tenders
 *
 * Public surface for the tenders module. Anything outside this folder
 * MUST import from `@/modules/tenders` (this file) — never reach into
 * `./service`, `./schema`, or `./policies` directly. ESLint enforces it.
 */
export {};
