/**
 * profiles · public types.
 *
 * Types used by both the service layer and any consumer (UI, jobs,
 * other modules). Keep DB row types in schema.ts; this file is for
 * derived / domain types.
 */
export {};
