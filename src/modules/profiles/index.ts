/**
 * @module profiles
 *
 * Public surface for the profiles module. Anything outside this folder
 * MUST import from `@/modules/profiles` (this file) — never reach into
 * `./service`, `./schema`, or `./policies` directly. ESLint enforces it.
 */
export {};
