/**
 * @module fba
 *
 * Public surface for the fba module. Anything outside this folder
 * MUST import from `@/modules/fba` (this file) — never reach into
 * `./service`, `./schema`, or `./policies` directly. ESLint enforces it.
 */
export {};
