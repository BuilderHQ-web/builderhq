/**
 * @module projects
 *
 * Public surface for the projects module. Anything outside this folder
 * MUST import from `@/modules/projects` (this file) — never reach into
 * `./service`, `./schema`, or `./policies` directly. ESLint enforces it.
 */
export {};
