/**
 * @module credits
 *
 * Public surface for the credits module. Anything outside this folder
 * MUST import from `@/modules/credits` (this file) — never reach into
 * `./service`, `./schema`, or `./policies` directly. ESLint enforces it.
 */
export {};
