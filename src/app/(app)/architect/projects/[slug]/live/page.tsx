/**
 * Architect mirror of the round-is-live landing — same handler, second
 * mount point (the architect IS the runner).
 */
export { default, metadata } from "../../../../owner/projects/[slug]/live/page";
export const dynamic = "force-dynamic";
