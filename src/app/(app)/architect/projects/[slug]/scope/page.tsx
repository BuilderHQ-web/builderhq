/**
 * Architect mirror of the tender pack review — same handler, second
 * mount point (the architect IS the runner).
 */
export { default, metadata } from "../../../../owner/projects/[slug]/scope/page";
export const dynamic = "force-dynamic";
