/**
 * Architect mount of the post-publish celebration. Same page as the
 * owner one; the page derives its link base from the session role, so
 * the onward links stay under /architect.
 */
export { default, metadata } from "../../../../owner/projects/[slug]/published/page";
