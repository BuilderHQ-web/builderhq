/**
 * Architect mirror of the runner-side Tender Document PDF route —
 * same handler, second mount point (architects run the same project
 * surfaces as owners).
 */
export { GET } from "../../../../../../owner/projects/[slug]/tenders/[tenderId]/document/route";
