import { Placeholder } from "@/components/brand/placeholder";

export const metadata = { title: "Builder dashboard" };

export default function BuilderDashboard() {
  return (
    <Placeholder
      eyebrow="Builder · dashboard"
      title="Browse & tender"
      description="The builder dashboard will surface matched projects, your unlocks, active tenders, and messages from owners. Founding Builder Access credit balance also lives here."
      phase="Phase 2–3 · marketplace + tenders"
    />
  );
}
