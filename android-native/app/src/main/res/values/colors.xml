<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Window background for cold start — cream in values/, navy in
         values-night/ — so the first frame matches the Compose theme
         (no flash). -->
    <color name="bhq_window_bg">#ECE7DD</color>
    <!-- Brand constants (launcher/splash art). Mode-independent. -->
    <color name="bhq_canvas">#06080F</color>
    <color name="bhq_accent">#00D4C8</color>
</resources>
